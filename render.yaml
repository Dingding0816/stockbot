services:
  - type: web
    name: stockbot-api
    env: python
    runtime: python3.10
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn api:app --host 0.0.0.0 --port 10000"

